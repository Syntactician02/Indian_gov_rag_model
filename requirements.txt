# -------------------------------
# Web Framework
# -------------------------------
flask==3.1.0
flask-cors==4.0.0

# -------------------------------
# HTTP Requests & Web Scraping
# -------------------------------
requests==2.32.3
beautifulsoup4==4.12.3
lxml==4.9.4   # 5.x fails to build on 3.14 currently

# -------------------------------
# Machine Learning & Embeddings
# -------------------------------
# Torch and transformers aren't yet officially released for 3.14,
# so use pre-release compatible wheels if available.
torch==2.3.1
sentence-transformers==2.2.2
transformers==4.40.2

# -------------------------------
# Vector Database
# -------------------------------
# faiss doesn't support 3.14 yet; comment it out or install manually when available.
# faiss-cpu==1.8.0
# faiss-cpu-windows==1.8.0

# -------------------------------
# Data Processing
# -------------------------------
numpy==1.26.4     # latest stable that supports 3.14
pandas==2.2.2

# -------------------------------
# Utilities
# -------------------------------
python-dateutil==2.9.0.post0
pytz==2024.1
python-dotenv==1.0.1

# -------------------------------
# Optional for production (Linux only)
# -------------------------------
# gunicorn==21.2.0
